import * as THREE from 'three';

// export function grabAnimation(object: THREE.Object3D, controller) {

// }

// new THREE.AnimationClip(
//   'grabbing', 0.5,
//   new THREE.VectorKeyframeTrack('.position[x]', [0, 0.5], [])
// )
